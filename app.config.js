"use strict";

window.Cortex = $.extend({}, window.Cortex, {
    "config": {
        "user": "user",
        "currentDevice": "iPhone"
    }
});
Object.preventExtensions(window.Cortex.config);
